- content_for(:body_id, 'template_flow')

%h1 Save as Template
- if @user.github_access_token_present?
  = render 'form', user: @user, template_form: @template_form

- else
  = render 'github_token_form', user: @user
