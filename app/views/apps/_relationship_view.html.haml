- app.ordered_services.each do |service|
  .relationship-panel
    %header
      = service.name
    - unless service.links.empty?
      %ul.links
        - service.links.each do |link|
          %li
            = link.service_name